# CHECKPOINT — текущее состояние проекта

> **Назначение.** Снимок на сегодня. Читается в начале каждого чата (третьим, после NAVIGATION и CORE_PRINCIPLES). За 30 секунд понять: где разработка, что было последним, с чего начинается сессия, какие грабли впереди.
>
> **Кто обновляет.** Антон / Claude в конце каждого дня недели — правило `PLAN.md`.
>
> **Связано.** Расписание — `PLAN.md`. Решения — `DECISIONS.md`. Константы — `FACTS.md`.

---

## 1. Текущее состояние [REQUIRED]

- **Дата обновления:** 2026-08-16
- **Текущая итерация:** Дни 1–4 закрыты. Следующий — день 5, «PDF и шевроны» (`PLAN.md §День 5`)
- **Период:** 2026-08-14 → 2026-08-21
- **Прогресс:** 4 / 7 дней закрыто
- **Ближайшая веха:** сдача заявки, 2026-08-21
- **До вехи:** 7 дней

---

## 2. Что сделано к текущему моменту [REQUIRED]

### 2.1. Готово

- **Рамка проекта** — `GOAL.md`, `PLAN.md`, четыре решения о модели, ставке, доставке и скоупе. Принято до старта кода, дальше immutable.
- **Обвязка** — пакет развёрнут полным профилем (`#D-05`): `core,guards,gates,quality,design-check,design-gen,agents,commands,skills,evals,scheduled,rules`. `migration` не взят — БД нет.
- **Пакет документации** — `NAVIGATION.md`, `CORE_PRINCIPLES.md`, `FACTS.md`, этот файл. Константы предметной области переехали из `DECISIONS.md` в `FACTS.md` как `#F-01`…`#F-10`.

- **День 1 — ядро** (коммит `d56d782`). Каркас Vite+TS+vitest, типы столярных операций, `simulate()` для `angleDeg = 90`, палитра пород из `#F-09`. Десять тестов зелёные, `tsc --noEmit` чист, сборка проходит. Гейты тестов и качества включены.
- **Вердикт по главной гипотезе** — `#D-06`: модель держит, внесены две правки (`Cut.kind`, `Strip.lengthMm`).
- **Деплой** — https://kudteam153-lab.github.io/endgrain/ отдаёт 200. Репозиторий публичный: `kudteam153-lab/endgrain`. Выкатывается сам на пуш в `main`, тесты и `tsc` стоят в workflow до сборки.
- **Условия конкурса разобраны** — `#F-11`…`#F-15`. Обязательный минимум сверен с планом, расхождение одно и закрыто решением `#D-08`.

- **День 2 — экран** (коммит `48d70cc`). Дизайн-система «Чертёж» (`#D-11`), SVG-доска с размерными линиями и нумерацией ламелей, панель параметров с мгновенным пересчётом, автосохранение в localStorage, экспорт SVG/PNG и проекта в JSON, видимый слой реализуемости (`#D-09`). Тринадцать тестов зелёные, `tsc` и оба скана `impeccable` чисты, тёмная тема и узкий экран проверены снимками.
- **Обязательный минимум конкурса** (`#F-11`) закрыт весь, кроме генеративности — она день 3.
- **Доводка дизайна по вердикту Антона** (`#D-12`): ахроматика вместо средних тонов, заголовок-монумент, нулевой радиус, штамп внутри SVG, сборка доски на экране.
- **Один экран без прокрутки** (`#D-13`): чертёж вписывается в лист, раскладка не дрожит при вводе. Штамп встаёт снизу или сбоку по форме бокса.
- **Печать браузером** (`#D-14`): кнопка «Печать», на бумагу уходит только лист. Не заменяет PDF дня 5.

- **День 3 — генератор.** Детерминированный ГСЧ от seed (`rng.ts`, mulberry32, свой — не зависимость). Рецепт описывает 1–3 уровня: до двух переклеек перед поперечным резом (`#D-15`). Грамматика `generate.ts`: палитра с контрастом соседей и одной экзотикой, три ритма ширин, глубина переклейки, узор под глубину. Галерея на двенадцать вариантов оверлеем, отложенные хранятся seed'ами. Пятьдесят три теста зелёные, `tsc` и `impeccable` чисты.
- **Грамматика проверена глазами** на двухстах seed'ах дважды — до и после ужесточения (`#D-16`). Ни отказа модели, ни предупреждения alarm, ни одного срыва в запасной вариант.

- **День 4 — расчёты** (`#D-17`). Лист 02 «Раскрой»: заготовки к покупке, порядок работ по шагам, смета по породам. Отход разведён на опилки, припуск на выравнивание и годный остаток — «не дошло до доски 59 %» пугало и врало. Единицы применены целиком: дюймы дробями по шестнадцатым, объём в board feet. Печать даёт комплект из двух листов. Семьдесят восемь тестов зелёные.

### 2.2. В работе

- **День 5** — PDF, угловой рез (шеврон и ёлочка), шаринг ссылкой.

### 2.3. Не начато

- Дни 5–7: PDF и шевроны, 3D и эволюция, сдача.

---

## 3. Текущая итерация — подробно [REQUIRED]

### 3.1. Цель дня 5

PDF-инструкция, угловой рез и шаринг ссылкой. PDF не режется ни при каких условиях (`CORE_PRINCIPLES.md §4.3`): без него отваливается главный критерий жюри и acceptance №3–4, остаётся красивая картинка.

### 3.2. В работе 🟡

| Задача      | Описание                                                             | Статус | Acceptance                                                              |
| ----------- | -------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------- |
| PDF         | оба листа плюс схема склеек по шагам; библиотека выбирается на месте | 🟡     | файл открывается, печатается, читается без приложения                   |
| Угловой рез | `angleDeg != 90` — шеврон и ёлочка. Сейчас модель отвергает их явно  | 🟡     | шеврон собирается и считается наравне с ортогональю, тесты на геометрию |
| Шаринг      | параметры и seed в URL-хеше, разбор недоверенного входа              | 🟡     | ссылка открывает ту же доску у другого человека                         |

### 3.3. Завершено ✅

| Задача                | Описание                                                         | Дата       |
| --------------------- | ---------------------------------------------------------------- | ---------- |
| Развёртывание обвязки | полный профиль, `#D-05`                                          | 2026-08-14 |
| Пакет документации    | NAVIGATION, CORE_PRINCIPLES, FACTS, CHECKPOINT                   | 2026-08-14 |
| День 1 — ядро         | типы, `simulate()`, пять узоров, вердикт `#D-06`                 | 2026-08-14 |
| День 2 — экран        | дизайн-система `#D-11`, рендер, ручки, сохранение, реализуемость | 2026-08-15 |
| День 3 — генератор    | ГСЧ, рецепт на 1–3 уровня `#D-15`, грамматика и вердикт `#D-16`  | 2026-08-16 |
| День 4 — расчёты      | лист 02 «Раскрой», смета, единицы, печать комплектом `#D-17`     | 2026-08-16 |

### 3.4. Под риском 🔄

| Задача             | Причина риска                                                                                                                                                                          |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Угловой рез        | Отдельная геометрия и отдельный класс ошибок раскроя. Стоит первым в списке реза скоупа (`CORE_PRINCIPLES.md §4.3`) — режется раньше всего остального, если PDF занял день целиком.    |
| Библиотека PDF     | Не выбрана (`CORE_PRINCIPLES.md §7`). Клиентская генерация без сервера (`#D-03`); риск — вес бандла и кириллические шрифты, которые часто отваливаются в PDF именно на русском тексте. |
| Смета при экзотике | Цены `#F-09` — рынок, а не константа. Доска с амарантом выходит за 20 тысяч: цифра верная, но выглядит как ошибка калькулятора. К PDF решить, показывать ли её с оговоркой.            |
| Качество генерации | Снят на дне 3 (`#D-16`). Остаётся следить, чтобы шеврон и эволюция дней 5–6 не вернули кашу: прогон seed'ов повторять после каждой правки грамматики.                                  |

---

## 4. Изменения в данных / схеме [optional]

N/A — БД нет (`#D-03`).

---

## 5. Изменения в API / контрактах [optional]

N/A — бэкенда нет (`#D-03`). Внешний контракт ровно один: формат URL-хеша с параметрами доски и seed. Появится на дне 5.

---

## 6. Архитектурные решения этой итерации [optional]

- `#D-05` — обвязка развёрнута полным профилем (см. `DECISIONS.md`)
- `#D-15` — рецепт описывает 1–3 уровня: переклейка стала частью узора
- `#D-16` — грамматика ужесточена по итогам просмотра 200 seed'ов
- `#D-17` — расчёты живут вторым листом, печать даёт комплект

---

## 7. Тех-долг, баги, риски [optional]

| Тип         | Описание                                                                                                                                                                                                                                                                     | Workaround                                         | Когда                                                   |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------- |
| долг Антона | `PLAN.md` не приведён к `#D-08`: сохранение проекта всё ещё числится днём 5, хотя сделано на дне 2. Файл immutable и защищён `guard-files.sh` — правку вносит Антон                                                                                                          | работа по решению выполнена                        | ближайшая сессия                                        |
| грабли      | Проверять деплой по коду ответа бессмысленно: при упавшем workflow сайт продолжает отдавать 200 предыдущей версией. Сверять `gh run list --json conclusion`                                                                                                                  | —                                                  | учтено 15.08                                            |
| тех-долг    | `puppeteer` заведён devDependency ради живого скана и снимков. В CI скачивание Chromium выключено `PUPPETEER_SKIP_DOWNLOAD` — там он не нужен                                                                                                                                | —                                                  | —                                                       |
| риск        | Точная дата и время сдачи организаторами не объявлены (`#F-15`). Работаем от 21.08.2026                                                                                                                                                                                      | публичная история коммитов доказывает сроки работы | заменить факт, когда объявят                            |
| риск        | Название репозитория `endgrain` выбрано мной, оно попадёт в заявку (`#F-12`). Переименование в GitHub безболезненно, но ломает уже разосланную ссылку                                                                                                                        | нет                                                | сказать сейчас, если не нравится                        |
| тех-долг    | Отложенные варианты хранятся seed'ами (`useFavourites`). Правка грамматики меняет доску под тем же seed — отложенное «портится». Осознанный размен: вторая копия раскладки разошлась бы с генератором. Правишь грамматику после дня 3 — предупреди, что отложенное изменится | нет                                                | до сдачи не чинить                                      |
| тех-долг    | Число плашек ограничено потолком в 400 (`recipe.ts`). Страховка от подвисания вкладки на абсурдном вводе; столяр этого потолка не увидит, но доска длиной 2 м с рядом в 1 мм молча станет короче заданной                                                                    | нет                                                | если появится время на дне 7 — показать предупреждением |
| тех-долг    | Линтера нет — роль гейта качества играет `tsc --strict` (`#D-07`). Стиль на неделю держится вручную                                                                                                                                                                          | нет                                                | после конкурса, если проект продолжится                 |
| риск        | `audit_refs.py` даёт битые ссылки на файлы, которых пока нет (`ROADMAP.md`, `DOMAIN_MODEL.md` в шаблонных текстах)                                                                                                                                                           | нет                                                | сверять только новые, не гнаться за нулём               |

---

## 8. Следующая микро-задача для Claude [REQUIRED] {#next-task}

### Что сделать

День 5 — PDF-инструкция, угловой рез, шаринг ссылкой. PDF первым: он не режется (`CORE_PRINCIPLES.md §4.3`), шеврон режется первым.

### Порядок

1. **PDF.** Оба листа уже свёрстаны (`#D-17`) — начать с попытки собрать PDF из них, а не верстать третий раз. Библиотека выбирается на месте; проверять на кириллице сразу, шрифты в PDF отваливаются именно на ней.
2. **Схема склеек по шагам.** В PDF нужна не только таблица: столяр смотрит на картинку раскладки перед каждой склейкой. Рисуется из тех же уровней модели, что и лист 01.
3. **Шаринг через URL-хеш.** Рецепт в хеш, разбор недоверенного входа — как в `parseRecipe` (`useRecipe.ts`), не `as Recipe`. Ссылка обязана открыть ту же доску.
4. **Угловой рез.** `simulate()` сейчас отвергает `angleDeg != 90` явным сообщением. Снять запрет и добавить геометрию шеврона. Остаётся день — делать, не остаётся — резать по списку и сказать вслух.

### Какие файлы трогать

- `src/render/pdf.ts` — новый: сборка PDF из листов
- `src/core/geometry.ts`, `src/core/simulate.ts` — угловой рез
- `src/state/useHash.ts` — новый: чтение и запись URL-хеша
- `src/ui/CutSheet.tsx` — схема склеек, если она встанет на лист 02

### Acceptance criteria

- [ ] PDF скачивается, открывается сторонним просмотрщиком, кириллица не рассыпалась
- [ ] В PDF есть чертёж, раскрой, схема каждой склейки и порядок действий
- [ ] Ссылка с хешем открывает ту же доску в другом браузере
- [ ] Шеврон собирается и считается наравне с ортогональю — либо честно вырезан из скоупа с записью в `DECISIONS.md`
- [ ] `npm run quality` и тесты зелёные, деплой прошёл (сверять `conclusion`, не код ответа)

### Ссылки на контекст

- День плана: `PLAN.md §День 5`
- Что не режется никогда: `CORE_PRINCIPLES.md §4.3`
- Печать и листы: `DECISIONS.md #D-14`, `#D-17`
- Тексты инструкции: `FACTS.md #F-05`, `#F-06`, `#F-10` (чего не предлагать)
- Разбор недоверенного входа: `src/state/useRecipe.ts`

### Оценка

~8 часов

---

## 9. Связанные артефакты

- **Репозиторий:** https://github.com/kudteam153-lab/endgrain (публичный)
- **Ссылка на продукт:** https://kudteam153-lab.github.io/endgrain/
- **Деплой:** GitHub Pages, `.github/workflows/deploy.yml`, автоматически на пуш в `main`

---

## 10. Что НЕ менять без явного запроса Антона

- `GOAL.md`, `PLAN.md` — рамка недели, защищены `guard-files.sh`. Скоуп режется по `CORE_PRINCIPLES.md §4.3`, а не правкой цели.
- Числа в `FACTS.md` — добыты ресёрчем, пересчёт «по памяти» портит заготовку в мастерской.
- Порядок реза скоупа (`CORE_PRINCIPLES.md §4.3`) — задан заранее, чтобы не выбирать в последний день под давлением.
- Правило «дизайн в CSS, инлайновых стилей в JSX нет» (`#D-10`) — снять его значит выключить гейт качества, оставив его на вид включённым.
- `.claude/settings.json` — хуки собраны вручную. Внешние установщики запускать только с `--no-hooks`.

---

_Конец. Обновляется в конце каждого дня недели._
