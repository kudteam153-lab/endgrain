/**
 * Виды детали в объёме.
 *
 * Лежат отдельно от `Board3d.tsx` намеренно: тот тянет за собой three.js, а
 * список видов нужен панели над листом, которая живёт в первом чанке. Импорт
 * из компонента затащил бы библиотеку на первый экран и отменил бы разделение
 * чанков (`#D-20`) — три сотни килобайт ради четырёх подписей на кнопках.
 *
 * Названия — проекции кульмана, а не термины камеры: столяр читает деталь
 * теми же видами, что и чертёж.
 */

export const VIEWS = [
  { id: "iso", name: "Изометрия", yaw: -0.5, pitch: 0.62 },
  { id: "top", name: "Сверху", yaw: 0, pitch: 1.5 },
  { id: "side", name: "Сбоку", yaw: 0, pitch: 0.05 },
  { id: "end", name: "С торца", yaw: Math.PI / 2, pitch: 0.05 },
] as const;

export type ViewId = (typeof VIEWS)[number]["id"];
