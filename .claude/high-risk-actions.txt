# =============================================================================
# high-risk-actions.txt — действия, требующие ЭСКАЛАЦИИ Антону до выполнения.
#
# Как используется: перед выполнением действия агент сверяется с этим списком.
# Совпало → НЕ делать молча: остановиться и вынести Антону выбор (A/B/C) с
# объяснением риска и альтернатив. Это поведенческий инвариант (AGENTS.md §4,
# CLAUDE.md §5).
#
# С 2026-07-26 часть категорий проверяется механически: те, что опознаются по
# самой команде или пути, вынесены в high-risk-patterns.txt, и guard-highrisk.sh
# отвечает по ним «ask» — эскалацию нельзя обойти накопленными разрешениями.
# Остальные механически не опознаются и остаются на дисциплине агента. Наличие
# механизма НЕ означает, что список закрыт: непокрытых категорий здесь больше,
# чем покрытых.
#
# Отличие от guard-bash.sh: guard-bash БЛОКИРУЕТ грубо-деструктивное (rm -rf /,
# force-push в main, DROP/TRUNCATE — решение «deny»). Этот список — про действия
# обратимые, но важные/дорогие, где нужно решение человека, а не блок.
#
# Формат: одна категория/паттерн на строку. Строки с # — комментарии.
# Список не исчерпывающий: при сомнении в необратимости/цене — эскалировать.
# =============================================================================

# --- данные и БД ---
# Миграции на проде / необратимые миграции (drop column, смена типа с потерей)
migration:prod
migration:irreversible
# Любое массовое удаление/перезапись данных (DELETE/UPDATE без узкого WHERE)
data:bulk-delete
data:bulk-update

# --- секреты и доступ ---
# Изменение боевых .env / ключей / токенов / секретов
secrets:prod-env
secrets:rotate-keys
# Изменение прав доступа, ролей, permission-матрицы в проде
access:prod-permissions

# --- деплой и инфраструктура ---
# Деплой/релиз на прод, изменение CI/CD, nginx, docker-compose в проде
deploy:prod
infra:ci-cd-change
infra:prod-config

# --- биллинг и коммерция ---
# Любые изменения тарифов, цен, платёжной логики, комиссий
billing:pricing-change
billing:payment-logic

# --- ломающие изменения контрактов ---
# Несовместимые изменения публичного API / схемы, ломающие клиентов
api:breaking-change
schema:breaking-change

# --- масштабные правки кода ---
# Рефактор/переименование, затрагивающие много файлов вне scope текущей задачи
code:mass-refactor
code:cross-codebase-rename

# --- обход контроля ---
# Отключение/обход хуков, guard-ов, тестов, gate-tests/gate-goal
control:bypass-guards
control:disable-tests

# --- VPN / ФЗ-152 (специфика твоих продуктов) ---
# Изменения, затрагивающие персональные данные пользователей, логи, хранение
privacy:user-data
privacy:retention-change
