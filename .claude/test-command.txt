# test-command.txt — contest (Вайбатон №1)
# Команда тестов для gate-tests.sh (Stop-хук). OPT-IN.
# Первая незакомментированная непустая строка = команда теста.
#
# Гейт включается в день 1, вместе с первым зелёным прогоном vitest — не позже.
# Вся ставка проекта (#D-01) в том, что узор выражается стеком столярных
# операций, и проверяют это ровно юнит-тесты на пять канонических узоров.
# Красный тест здесь означает не «сломалась деталь», а «модель не держит».
#
# Включено 14.08.2026: vitest стоит, десять тестов зелёные, прогон 0.2 с.

npx vitest run --silent
